#include <iostream>

using namespace std;

int main(){

    int n;
    cin >> n;
    int cow[n][2];
    int rank[n];
    for (int i = 0; i < n; i++){
        cin >> cow[n][0] >> cow[n][1];
        rank[i] = cow[]
    }


    return 0;
}